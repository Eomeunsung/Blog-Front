import {toolbarOptions} from "../quill/editor/ToobarOption";

export const modules = {
    toolbar: toolbarOptions.toolbar,
    imageActions: toolbarOptions.imageActions,
    imageFormats: toolbarOptions.imageFormats
};